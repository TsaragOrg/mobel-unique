# Draft Specs

Use this directory for specs that are still being shaped.

Draft specs can change freely. Once accepted, move the spec to `docs/specs/accepted` and register it in `docs/specs/manifest.json`.

